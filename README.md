# Secure-Banking-System

### Terminal 1, start server (from server/ folder)
`cd server && python server_main.py`
but now server GUI added:
`cd server && python server_gui.py`
### Terminal 2, launch ATM GUI
`cd client/gui && python app.py`
### Terminal 3, second ATM client
`cd client/gui && python app.py`